<?php

/**
 * @file
 * Local-only secrets and overrides. Not committed (see .gitignore).
 *
 * Copy this file in place and edit as needed. Anything set here takes
 * precedence over the layered settings under ../all/ because the
 * site-level settings.php sources it last.
 *
 * Examples:
 * - $settings['hash_salt'] = '...';
 * - $config['ai_provider_openai.settings']['api_key'] = '...';
 * - $databases['default']['default']['host'] = '127.0.0.1';
 */
