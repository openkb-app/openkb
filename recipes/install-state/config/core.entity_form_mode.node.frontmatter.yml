langcode: en
status: true
dependencies:
  module:
    - node
id: node.frontmatter
label: Frontmatter
description: 'Exposure contract for the OpenKB frontmatter surface: fields placed in this form display are published via /openkb/schema, in display order, with widget settings as rendering hints.'
targetEntityType: node
cache: true
