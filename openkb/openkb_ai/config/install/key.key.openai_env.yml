langcode: en
status: true
dependencies: {  }
id: openai_env
label: 'OpenAI API key (environment)'
description: 'Read from the OPENAI_API_KEY environment variable, so the key is never stored in config or the database. Empty where the environment sets no variable.'
key_type: authentication
key_type_settings: {  }
key_provider: env
key_provider_settings:
  env_variable: OPENAI_API_KEY
  base64_encoded: false
  strip_line_breaks: true
key_input: none
key_input_settings: {  }
