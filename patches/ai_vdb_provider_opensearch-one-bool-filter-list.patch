diff --git a/src/Plugin/VdbProvider/OpenSearchVdbProvider.php b/src/Plugin/VdbProvider/OpenSearchVdbProvider.php
index 2304f6b..e76e948 100644
--- a/src/Plugin/VdbProvider/OpenSearchVdbProvider.php
+++ b/src/Plugin/VdbProvider/OpenSearchVdbProvider.php
@@ -377,10 +377,16 @@ class OpenSearchVdbProvider extends AiVdbProviderClientBase implements Dependent
     $filters = $this->filterBuilder->buildFilters($conditionGroup, $query->getIndex()
       ->getFields());
 
-    // Filter to only items from the current index.
-    $filters['term']['index_id'] = $query->getIndex()->id();
+    // Every clause goes in its own entry of one bool filter list. A query
+    // object holds a single clause, so writing the index into the clause the
+    // condition group produced would either put two fields in one `term` or
+    // put a `term` beside a `bool`, and OpenSearch rejects both.
+    $clauses = [['term' => ['index_id' => $query->getIndex()->id()]]];
+    if (!empty($filters)) {
+      array_unshift($clauses, $filters);
+    }
 
-    return $filters;
+    return ['bool' => ['filter' => $clauses]];
   }
 
   /**
