# The CI and local-development defaults: `scripted` is the mode an end-to-end
# run steers, and the pacing is what makes a buffering hop visible as an answer
# that lands whole.
mode: scripted
delay_ms: 40
answer: |-
  Mock answer for:

  @prompt

  The knowledge base covers onboarding, the system architecture and the authoring workflow.
