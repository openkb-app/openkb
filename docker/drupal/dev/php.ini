; Development overrides, read by the server and the CLI alike. Selected by
; PHP_INI_SCAN_DIR in docker-compose.development.yml.
display_errors = On
display_startup_errors = On
opcache.validate_timestamps = 1
zend.assertions = 1
error_reporting = E_ALL
max_execution_time = 900
memory_limit = 512M
