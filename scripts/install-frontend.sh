#!/usr/bin/env bash
# Frontend installer stub.
#
# The Nuxt frontend lives in ./frontend/ and is committed to this monorepo,
# so there is nothing to clone / pull. Kept as an entry point so `phapp build`
# and future CI calls keep working.
set -e
echo "[install-frontend] frontend committed under frontend/, nothing to do"
exit 0
